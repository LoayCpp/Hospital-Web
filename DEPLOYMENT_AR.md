# نشر النظام على Neon وVercel

أصبحت هذه النسخة تدعم وضعين:

- تشغيل محلي قديم: عند فتحها عبر الخادم المحلي وعدم وجود API تبقى البيانات داخل المتصفح.
- تشغيل سحابي: على Vercel تحمى البيانات بكلمة مرور، وتحفظ في Neon عبر Vercel Functions. لا يصل DATABASE_URL إلى المتصفح.

> تنبيه: النظام يتعامل مع بيانات تشغيلية صحية وأرقام تواصل. قبل استخدام بيانات فعلية، احصل على موافقة أمن المعلومات والخصوصية في الجهة وحدد سياسة الصلاحيات والنسخ الاحتياطي والاحتفاظ بالبيانات. كلمة المرور المشتركة مناسبة لنسخة أولية صغيرة، وليست بديلًا عن حساب مستقل وصلاحية لكل مستخدم وسجل تدقيقي.

بعد اكتمال النشر، الرابط وكلمة المرور المشتركة يفتحان الحالة نفسها لجميع المستخدمين. كل قراءة وحفظ يمر عبر Vercel إلى Neon، لذلك لا يتطلب بقاء جهاز المنشئ قيد التشغيل، وتبقى البيانات بعد إغلاق الرابط أو الجهاز.

## 1. احفظ البيانات الموجودة

من النسخة المحلية الحالية افتح «البيانات والنسخ الاحتياطي» ثم نزّل «نسخة احتياطية» بصيغة JSON. بيانات localStorage لا تنتقل تلقائيًا إلى نطاق Vercel الجديد.

لا تبدأ بإدخال بيانات فعلية في الموقع المنشور قبل إكمال الحماية والاختبار.

## 2. ثبّت الحزم واختبر المشروع

من المجلد الذي يحتوي package.json:

    npm install
    npm run check
    npm test
    npm run build

الناتج الثابت ينشأ في dist. لا تستخدم serve.mjs كخادم إنتاج؛ Vercel يخدم الملفات الثابتة ويشغّل الملفات داخل api كـ Functions.

## 3. أنشئ Neon واربطه بـ Vercel

الطريقة الأسهل هي تكامل Neon الرسمي في Vercel Marketplace:

1. أنشئ مشروع Vercel أو اربط هذا المجلد بالمشروع.
2. افتح Marketplace في Vercel واختر Neon.
3. أنشئ قاعدة جديدة أو اربط حساب Neon موجودًا.
4. تأكد من إضافة DATABASE_URL المجمّع (اسم المضيف يحتوي غالبًا على -pooler) إلى Production وPreview وDevelopment للاختبار المحلي.
5. أضف الاتصال المباشر باسم DATABASE_URL_UNPOOLED عند تشغيل migrations إن أتاحه التكامل.

من CLI يمكن بدء الربط بالأوامر التالية بعد تسجيل الدخول:

    npx vercel link
    npx vercel integration add neon
    npx vercel env pull .env.local

اختر منطقة Neon قريبة من منطقة Vercel Functions. لا تضع DATABASE_URL في src ولا تسمّه باسم يبدأ بـ NEXT_PUBLIC_.

## 4. أضف أسرار الدخول

في Vercel > Project Settings > Environment Variables أضف إلى Production وPreview وDevelopment:

- APP_PASSWORD: كلمة مرور طويلة وفريدة (يفضل 16 حرفًا أو أكثر).
- SESSION_SECRET: قيمة عشوائية لا تقل عن 32 حرفًا.
- DATABASE_URL: يضيفها تكامل Neon عادةً.

لتوليد SESSION_SECRET:

    node -e "console.log(require('node:crypto').randomBytes(32).toString('base64url'))"

لا ترفع .env.local إلى Git؛ الملف مستبعد في .gitignore.

## 5. أنشئ جدول Neon

بعد تنفيذ vercel env pull .env.local:

    npm run db:init

بديلًا عن ذلك، افتح SQL Editor في Neon وشغّل محتوى db/schema.sql. الأمر آمن عند تكراره ولا يحذف بيانات. أعد تشغيله أيضًا عند تحديث نسخة منشورة سابقًا، لأنه ينشئ جداول سجل المراجعات وتحديد محاولات الدخول الجديدة.

## 6. انشر Preview ثم Production

إعدادات المشروع المقترحة:

- Framework Preset: Other
- Root Directory: نقطة إذا كان package.json في جذر المستودع. إذا رفعت المجلد الخارجي، اختر qassim-critical-cases-tracker.
- Build Command: npm run build
- Output Directory: dist

للمعاينة:

    npx vercel deploy

اختبر تسجيل الدخول، إنشاء سجل، تحديث الصفحة، التصدير، والاستيراد. بعد نجاح المعاينة:

    npx vercel deploy --prod

تغيير أي Environment Variable لا يؤثر في نشر قديم؛ أعد النشر بعد التغيير.

## 7. انقل البيانات القديمة

1. افتح رابط Vercel وسجّل الدخول.
2. افتح «البيانات والنسخ الاحتياطي».
3. اختر «استعادة نسخة احتياطية» وحدد ملف JSON الذي نزّلته في الخطوة الأولى.
4. انتظر حتى يظهر في الشريط الجانبي «متصل بقاعدة Neon».
5. حدّث الصفحة وتحقق من أعداد الأيام والسجلات والمستشفيات.
6. نزّل نسخة احتياطية جديدة بعد التحقق.

قاعدة Neon الجديدة تبدأ بلا سجلات؛ البيانات المرجعية/التجريبية الموجودة في وضع التشغيل المحلي لا ترفع تلقائيًا.

## كيف تعمل النسخة السحابية

- api/login.js وapi/logout.js: جلسة Cookie موقعة، HttpOnly وSecure.
- api/state.js: قراءة وحفظ الحالة من Neon.
- server: التحقق، الحماية، تحديد محاولات الدخول، والاتصال بقاعدة البيانات.
- db/schema.sql: مخطط الحالة، سجل المراجعات، وجدول تحديد محاولات الدخول.
- src/storage.js: نسخة محلية مؤقتة ومزامنة مؤجلة عند انقطاع الاتصال.

كل حفظ يحمل رقم مراجعة. إذا عدّل جهاز آخر البيانات أولًا يعيد الخادم 409 ولا يستبدل التغييرات بصمت؛ تظهر رسالة تطلب إعادة التحميل والمراجعة.

قبل كل تحديث تُحفظ النسخة السابقة في app_state_revisions للمساعدة في الاسترجاع الفني. هذا سجل نسخ، وليس سجل تدقيق كاملًا يحدد هوية المستخدم.

## حدود النسخة الأولى

- تستخدم كلمة مرور مشتركة ولا تميز المستخدم الذي أجرى التعديل، رغم وجود سجل للنسخ السابقة.
- تخزن حالة التطبيق في صف JSONB واحد لتقليل تغيير الواجهة الحالية. هذا مناسب للاستخدام المحدود، لكنه ليس التصميم النهائي لعدد كبير من المستخدمين المتزامنين.
- قبل التوسع يوصى بفصل السجلات اليومية إلى جدول مستقل، إضافة حسابات وصلاحيات وسجل تدقيقي بالهوية، تعزيز الحماية عبر Vercel Firewall، نسخ احتياطي/استعادة مجرب، وسياسة احتفاظ معتمدة.

## روابط رسمية

- [Neon على Vercel](https://vercel.com/marketplace/neon/neon)
- [Postgres على Vercel](https://vercel.com/docs/postgres)
- [Neon serverless driver](https://neon.com/docs/serverless/serverless-driver)
- [إدارة متغيرات Vercel](https://vercel.com/docs/environment-variables)
- [النشر عبر Vercel CLI](https://vercel.com/docs/projects/deploy-from-cli)
