{
  "name": "qassim-critical-cases-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "نظام محلي لمتابعة تحديثات الحالات الحرجة والوفاة الدماغية",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check serve.mjs && node --check src/rules.js && node --check src/storage.js && node --check src/xlsx.js && node --check src/app.js && node --check scripts/build.mjs && node --check scripts/init-db.mjs && node --check server/auth.js && node --check server/db.js && node --check server/http.js && node --check server/rate-limit.js && node --check server/validate.js && node --check api/login.js && node --check api/logout.js && node --check api/state.js",
    "build": "node scripts/build.mjs",
    "db:init": "node --env-file=.env.local scripts/init-db.mjs",
    "vercel-build": "npm run build",
    "dev": "node serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
